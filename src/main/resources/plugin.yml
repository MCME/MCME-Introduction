name: '${plugin.name}'
version: '${plugin.version}'
main: com.mcmiddleearth.introduction.paper.IntroductionPlugin
api-version: 1.21
authors: [ ${plugin.author} ]
depends: [ MCME-Architect, ViaVersion, ProtocolLib ]
description: ${plugin.description}

permissions:
  introduction.reload:
    default: op
    description: allows to reload plugin data.